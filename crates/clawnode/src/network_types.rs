//! Network mesh types — re-exports from the `claw-network` crate.

pub use claw_network::{
    AllocatorStats, IpAllocator, MeshConfig, MeshNode, MeshNodeBuilder, MeshTopology, NodeId,
    Region, WireGuardKey, WireGuardMesh,
};
